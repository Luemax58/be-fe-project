{
	"image": "mcr.microsoft.com/devcontainers/universal:2",

	// เราจะย้ายไปใช้ docker-compose.yml
	// "postCreateCommand": "..."  <-- ลบทิ้ง
	// "mounts": [ ... ]           <-- ลบทิ้ง

	// สั่งให้ VSCode ติดตั้ง Extension ที่จำเป็น
	"customizations": {
		"vscode": {
			"extensions": [
				"golang.go",
				"ms-azuretools.vscode-docker"
			]
		}
	}
}